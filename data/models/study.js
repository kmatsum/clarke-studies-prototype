//Data Structure for the STUDY data type
export default class Study {
    constructor (
        studyNum,
        titleImage,
    ) {
        this.studyNum = studyNum;
        this.titleImage = titleImage;
    }
}